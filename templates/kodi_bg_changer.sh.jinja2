#!/bin/bash

NEWNAME=$(
  find . -maxdepth 1 -name "*.jpg" -type f -printf "%f\n" | \
  while read x; do
    echo "$((RANDOM % 1000)):$x"
  done | sort -n | sed 's/[0-9]*://' | head -1
)

echo "Linking $NEWNAME to dynamic_bg.jpg"

ln -fs $NEWNAME dynamic_bg.jpg
